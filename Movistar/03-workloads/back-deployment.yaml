apiVersion: apps/v1
kind: Deployment
metadata:
  name: reto-movistar-back
  labels:
    app.kubernetes.io/name: reto-movistar
    app.kubernetes.io/component: back
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: reto-movistar
      app.kubernetes.io/component: back
  template:
    metadata:
      labels:
        app.kubernetes.io/name: reto-movistar
        app.kubernetes.io/component: back
    spec:
      serviceAccountName: reto-movistar
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 30
      imagePullSecrets:
        - name: ghcr-pull-secret
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: back
          image: ghcr.io/gabrielwyp/reto-movistar-back:bootstrap
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: reto-movistar-config
            # Absent until object storage is configured; the run audit is then disabled.
            - secretRef:
                name: reto-movistar-oci
                optional: true
          env:
            - name: SONIA_STORAGE_ROOT
              value: /var/lib/sonia
            - name: OPENCODE_KEY
              valueFrom:
                secretKeyRef:
                  name: reto-movistar-opencode
                  key: OPENCODE_KEY
                  optional: true
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          startupProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 30
            periodSeconds: 5
            timeoutSeconds: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: sonia-live
              mountPath: /var/lib/sonia
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: sonia-live
          persistentVolumeClaim:
            claimName: sonia-live
        - name: tmp
          emptyDir:
            sizeLimit: 64Mi
